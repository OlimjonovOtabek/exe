You are analysing why a Claude Code subscription is burning through its usage limit. Below is a JSON summary computed locally from the user's own transcripts for the current rate-limit window, plus the live limit state. Numbers are exact; do not invent others.

Rank the causes by their share of consumption, cite the numbers that prove each one, and give three to five concrete actions the user can apply today, each with the command or setting to change and a rough expected saving. Prefer the levers that cost nothing in quality: clearing context between tasks, smaller tool outputs, lower effort for routine work, cheaper models for chores, disabling unused plugins, cache hygiene. Mention the model mix, thinking share, cache hit ratio, largest tool results, subagent share and plugin context cost only when the numbers make them material.

Write the Telegram text in plain sentences with a short bullet list, under 1500 characters, no markdown, no emoji. Do not restate the whole summary.
